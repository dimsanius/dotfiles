# - Run current script: ansible-playbook ./setup.yml --ask-become-pass
# - For dev: install ansible-lint: pip install ansible-lint
- name: Machine setup
  hosts: localhost
  connection: local
  gather_facts: true

  tasks:
    - name: General | Gathering current user facts
      ansible.builtin.set_fact:
        remote_regular_user: "{{ ansible_user_id }}"

    - name: General | Installing packages
      become: true
      ansible.builtin.apt:
        name:
          - git
          - curl
          - wget
          - gpg
          - zsh
          - vim
          - neofetch
          - htop
          - jq
          - tree
          - ca-certificates
          - apt-transport-https
          - fzf
          - tig
          - exa
          - nala
          - timeshift
          - ncdu
          - bat
          - tldr
          - ripgrep
          - btop
          - mc
          # Below are packages for non-headless installs
          - terminator
          - gnome-shell-extensions
          - gnome-tweaks
        state: present
    
    - name: OMZ | Checking if Oh-My-zsh is already exists
      stat:
        path: ~/.oh-my-zsh
      register: oh_my_zsh_install

    - name: OMZ | Installing Oh-My-zsh
      when: not oh_my_zsh_install.stat.exists
      ansible.builtin.shell: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended --keep-zshrc


    - name: OMZ | Changing default shell to zsh
      become: true
      user: 
        name: "{{ remote_regular_user }}"
        shell: /usr/bin/zsh

    - name: OMZ Addons | Installing zsh-syntax-highlighting addon
      ansible.builtin.git:
        repo: 'https://github.com/zsh-users/zsh-syntax-highlighting.git'
        dest: ~/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting
        depth: 1
    
    - name: OMZ Addons | Installing zsh-autosuggestions addon
      ansible.builtin.git:
        repo: 'https://github.com/zsh-users/zsh-autosuggestions.git'
        dest: ~/.oh-my-zsh/custom/plugins/zsh-autosuggestions
        depth: 1

    - name: OMZ Addons | Installing zsh-auto-notify addon
      # Only in non-headless distros
      ansible.builtin.git:
        repo: 'https://github.com/MichaelAquilina/zsh-auto-notify.git'
        dest: ~/.oh-my-zsh/custom/plugins/auto-notify
        depth: 1

    - name: OMZ Addons | Installing zsh-you-should-use addon
      ansible.builtin.git:
        repo: 'https://github.com/MichaelAquilina/zsh-you-should-use.git'
        dest: ~/.oh-my-zsh/custom/plugins/you-should-use
        depth: 1

    - name: OMZ Addons | Installing zsh-completions addon
      ansible.builtin.git:
        repo: 'https://github.com/zsh-users/zsh-completions.git'
        dest: ~/.oh-my-zsh/custom/plugins/zsh-completions
        depth: 1
    
    - name: OMZ Addons | Installing powerlevel10k theme for zsh
      ansible.builtin.git:
        repo: 'https://github.com/romkatv/powerlevel10k.git'
        dest: ~/.oh-my-zsh/custom/themes/powerlevel10k
        depth: 1

    - name: vim | Installing vim-plug
      ansible.builtin.shell: curl -sfLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
    
    - name: vim | Installing vim plugins
      ansible.builtin.shell: vim -u ~/.vimrc -e -s +PlugInstall +qall

    - name: Brave | Checking if Brave Browser keyring exists
      ansible.builtin.stat:
        path: /usr/share/keyrings/brave-browser-archive-keyring.gpg
      register: brave_keyring_file

    - name: Brave | Getting Brave Browser keyring file
      become: true
      ansible.builtin.get_url:
        url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
        dest: "/usr/share/keyrings/brave-browser-archive-keyring.gpg"
        mode: "0644"
      when: not brave_keyring_file.stat.exists

    - name: Brave | Adding Brave repository to list of repositories
      become: true
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/brave-browser-release.list
        content: |
          deb [arch=amd64 signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main
        mode: "0644"

    - name: Brave | Installing Brave
      become: true
      ansible.builtin.apt:
        name: brave-browser
        state: present
        update_cache: true

    - name: VS Code | Checking if Microsoft keyring exists
      ansible.builtin.stat:
        path: /etc/apt/keyrings/packages.microsoft.asc
      register: microsoft_keyring_file

    - name: VS Code | Getting Microsoft keyring file
      become: true
      ansible.builtin.get_url:
        url: https://packages.microsoft.com/keys/microsoft.asc
        dest: "/etc/apt/keyrings/packages.microsoft.asc"
        mode: "0644"
      when: not microsoft_keyring_file.stat.exists

    # TODO: Dearmor key, avoid using trusted
    - name: VS Code | Adding VS Code repository to list of repositories
      become: true
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64,arm64,armhf
          signed-by=/etc/apt/keyrings/microsoft.asc
          trusted=yes]
          https://packages.microsoft.com/repos/code stable main
        filename: vscode
        state: present

    - name: VS Code | Installing VS Code
      become: true
      ansible.builtin.apt:
        name: code
        state: present
        update_cache: true

    - name: Sublime Text | Checking if Sublime Text keyring exists
      ansible.builtin.stat:
        path: /etc/apt/trusted.gpg.d/sublimehq-archive.gpg
      register: sublime_text_keyring_file

    - name: Sublime Text | Getting Sublime Text keyring file
      become: true
      ansible.builtin.get_url:
        url: https://download.sublimetext.com/sublimehq-pub.gpg
        dest: "/etc/apt/trusted.gpg.d/sublimehq-archive.gpg"
        mode: "0644"
      when: not sublime_text_keyring_file.stat.exists


    # TODO: Dearmor key, avoid using trusted
    - name: Sublime Text | Adding Sublime Text repository to list of repositories
      become: true
      ansible.builtin.apt_repository:
        repo: >-
          deb [trusted=yes] https://download.sublimetext.com/ apt/stable/
        filename: sublime-text
        state: present

    - name: Sublime Text | Installing Sublime Text
      become: true
      ansible.builtin.apt:
        name: sublime-text
        state: present
        update_cache: true

    - name: zoxide | Downloading zoxide
      ansible.builtin.get_url:
        url: https://raw.githubusercontent.com/ajeetdsouza/zoxide/main/install.sh
        dest: /tmp/zoxide_install.sh
        mode: '0755'

    - name: zoxide | Installing zoxide
      ansible.builtin.command: /tmp/zoxide_install.sh

    - name: General | Clearing font cache
      ansible.builtin.shell: fc-cache -f -v >/dev/null

    - name: timeshift | Setting timeshift (via GUI)
    # Requires user input, hence running last
      become: true
      ansible.builtin.command: timeshift-gtk