# SPDX-License-Identifier: MIT-0
---
# tasks file for roles/apt_repositories
- name: Add APT repositories
  become: true
  ansible.builtin.deb822_repository:
    state: present
    name: "{{ item.name }}"
    types: deb
    uris: "{{ item.uris }}"
    suites: "{{ item.suites }}"
    components: "{{ item.components | default(omit) }}"
    architectures: amd64
    signed_by: "{{ item.signed_by }}"
  loop: "{{ apt_repositories_repos }}"

- name: Update APT cache
  become: true
  ansible.builtin.apt:
    # Explicitly updating cache.
    # This avoids running cache updates for each APT repository
    update_cache: true

- name: Install packages
  become: true
  ansible.builtin.apt:
    name: "{{ item.package }}"
    state: present
  loop: "{{ apt_repositories_repos }}"
