# SPDX-License-Identifier: MIT-0
---
# tasks file for roles/timeshift
- name: Start timeshift (via GUI)
  # This task requires user input to finish
  become: true
  ansible.builtin.command: timeshift-gtk
  async: 3600
  poll: 0
  changed_when: false
