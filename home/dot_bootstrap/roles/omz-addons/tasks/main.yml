#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/omz-addons
- name: Install OMZ addons and themes
  ansible.builtin.git:
    repo: "{{ item.repo }}"
    dest: "{{ ansible_env.HOME }}/.oh-my-zsh/custom/plugins/{{ item.dest }}"
    depth: 1
  loop: "{{ omz_addons }}"
